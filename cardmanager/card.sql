
CREATE TABLE CARD(
   cardno      INTEGER NOT NULL primary key auto_increment,
   name       VARCHAR(100),
   phone        VARCHAR(100),
   companyname    VARCHAR(100),
   carddate     VARCHAR(100)
);
 

